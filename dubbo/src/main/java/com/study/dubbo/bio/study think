1、先启动服务器，服务器阻塞在accept方法，一旦启动客户端，于是服务器便会执行
ChatHandle方法，然后阻塞在readLine这个方法上；

2、客户端循环执行readLine等待用户即将输入的数据，然后通过native方法
拿到读到的数据，同时将其写入Buffer里，同时服务端也是阻塞在readLine里
面，一旦有数据写入，便会立刻做出反应，发送给其它客户端；

3、其它客户端同样也阻塞在readLine，一旦有消息便打印出来；

4、一旦断开，服务器的readline方法便会抛出异常，catch到错误后，finally
打印出关闭客户端的提示效果。